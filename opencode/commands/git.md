---
name: git
description: Apply the git-workflow skill and help with Git tasks — branch naming, conventional commits, push workflow, PRs, and gh CLI usage.
---

Apply the git-workflow skill and help me with the following Git task:

$ARGUMENTS

Follow the git-workflow skill conventions:
- Use conventional commit format
- Show staged files and proposed commit message before committing
- Wait for explicit approval before running `git commit` or `git push`
- Never push to main/master directly
- Check PR size thresholds if creating a pull request
